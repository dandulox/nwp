{
  "name": "netzwerkplaner",
  "version": "1.0.0",
  "description": "Visueller Netzwerkplaner mit Subnetting und VLAN-Management",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["netzwerk", "planer", "subnetting", "vlan", "visual"],
  "author": "Netzwerkplaner Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
